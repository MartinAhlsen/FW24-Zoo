const Mammals = () => {
  return <h1>Mammals</h1>;
};

export default Mammals;

{
  /* <div>
        {animals.map((mammal, index) => (
          <div>
            {index}. {mammal.name}
          </div>
        ))}
      </div> */
}
